#include <stdio.h>
#include <string.h>
#include <cs50.h>

#define MAX_LENGTH 4

int main(void)
{
    char guess[MAX_LENGTH];
    
    
    for (int x = 0; x < MAX_LENGTH; x++)
    {
         for (int i = 0; i  < 26; i++)
        {
            guess[count_i] = 'a' + i;
        }
        count_i++;
    }
   
    
    
    return 0;
}